#write a py program to read the string as a input from the user and replace all vowels with zero's
str=input("Enter the input: ")
for i in str:
    if str in   'AEIOUaeiou':
        str1=str.replace()
        print(str1)
        